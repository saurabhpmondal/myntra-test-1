/**
 * =====================================================
 * Project Phoenix
 * Phoenix Framework
 * File : render.js
 * Purpose : Render Engine
 * =====================================================
 */

export function render(target, element) {

    target.replaceChildren(element);

}
